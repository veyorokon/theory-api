# pytest.ini (repo root)
[pytest]
addopts = -q --strict-markers
testpaths = tests

markers =
    unit: fast hermetic tests (no I/O, no DB, pure Python)
    contracts: protocol invariants and schema validation
    integration: adapter + tool invocation (requires containers or Modal)

filterwarnings =
    ignore::DeprecationWarning
